## Day 3 — Create Your First Django Project

> **Project:** CampusQuest — Student Skill & Challenge Platform  
> **Course:** Django Zero to Hero  
> **Day:** 3  
> **Operating System:** Windows  

# PART A — UNDERSTANDING DJANGO

# 1. What Is Django?

Django is a:

> **High-level Python web framework used to build web applications quickly and securely.**

Django is written in Python.

Our technology stack is:

```text
Python
   ↓
Django
   ↓
CampusQuest
   ↓
Web Application / REST API
```

---

# 2. What Is a Web Framework?

Before Django, imagine we build a website completely from scratch.

We would need to handle:

```text
URL routing
HTML responses
Database communication
User authentication
Forms
Security
Sessions
Admin interface
Application structure
```

That is a lot of work.

A framework provides ready-made tools and conventions.

```text
Without Framework

Python
   ↓
Build everything yourself
   ↓
Lots of code
   ↓
More complexity


With Django

Python
   ↓
Django
   ↓
Ready-made web development tools
   ↓
Faster development
```

---

# 3. Why Django?

Django provides many features out of the box.

Important features include:

- URL routing
- Views
- Templates
- ORM
- Models
- Forms
- Authentication
- Sessions
- Admin panel
- Security features
- Middleware
- Static files support
- Database integration
- Application structure
- REST API support through Django REST Framework

---

# 4. Django Is Batteries-Included

You will often hear:

> **Django is a batteries-included framework.**

This means Django provides many commonly required web-development features instead of forcing developers to build everything from scratch.

For example:

```text
Django
├── URL routing
├── Views
├── Templates
├── ORM
├── Authentication
├── Admin
├── Forms
├── Sessions
├── Security
└── Middleware
```

---

# 5. Django's General Architecture

Django is commonly explained using:

> **MVT — Model, View, Template**

```text
              USER
               |
               v
              URL
               |
               v
              VIEW
             /    \
            /      \
           v        v
        MODEL    TEMPLATE
           |        |
           v        v
       DATABASE   HTML
```

We will learn MVT in detail in later days.

For Day 3, remember:

```text
Model    → Data / Database
View     → Business logic
Template → User interface
```

---

# 6. Django Project vs Django Application

This is one of the most important concepts.

## Django Project

A **project** is the complete website/application configuration.

Example:

```text
CampusQuest
```

## Django Application

An **app** is a specific functional component inside the project.

For example:

```text
CampusQuest
│
├── accounts
├── challenges
├── leaderboard
├── notifications
└── core
```

Think:

```text
Project
   ↓
CampusQuest

Apps
   ↓
Accounts
Challenges
Leaderboard
Notifications
```

We will create our first application on **Day 4**.

---

# 7. CampusQuest — Our Long-Term Project

Our project is:

# 🎓 CampusQuest

A student skill and challenge platform.

Students will eventually be able to:

```text
Register
   ↓
Login
   ↓
Create profile
   ↓
Explore challenges
   ↓
Submit solutions
   ↓
Earn points
   ↓
Climb leaderboard
   ↓
Receive notifications
```

Later our project may contain:

```text
CampusQuest
│
├── accounts
├── challenges
├── leaderboard
├── submissions
├── notifications
└── core
```

But today we create only the **Django project foundation**.

---

# PART B — CREATE THE DJANGO PROJECT

# 8. Open Command Prompt

Open:

```text
Command Prompt
```

Do not use PowerShell for this course.

---

# 9. Go to CampusQuest

Navigate to the project directory:

```cmd
cd C:\Projects\CampusQuest
```

You should be inside:

```text
C:\Projects\CampusQuest
```

---

# 10. Activate the Existing Environment

Day 2 already covered environments, so we won't repeat the explanation.

Activate:

```cmd
venv\Scripts\activate
```

You should see:

```text
(venv) C:\Projects\CampusQuest>
```

---

# 11. Verify Django

Run:

```cmd
python -m django --version
```

Example:

```text
5.x.x
```

The exact version depends on the version installed in your environment.

---

# 12. What Is `django-admin`?

`django-admin` is Django's command-line utility.

It helps us perform Django project-level tasks.

For example:

```cmd
django-admin startproject
```

creates a Django project.

Think:

```text
django-admin
      |
      +── startproject
      +── startapp
      +── check
      +── ...
```

We will use several Django commands throughout the course.

---

# 13. Create the Django Project

We are currently here:

```text
C:\Projects\CampusQuest
```

Run:

```cmd
django-admin startproject config .
```

This is an important command, so let's understand every part.

---

# 14. Understanding the Command

```cmd
django-admin startproject config .
```

Break it down:

```text
django-admin
```

Django command-line utility.

```text
startproject
```

Tell Django to create a new project.

```text
config
```

Name of the Django configuration package.

```text
.
```

Create the project in the current directory.

---

# 15. Why Are We Using `config`?

We could create:

```cmd
django-admin startproject CampusQuest .
```

But we are using:

```cmd
django-admin startproject config .
```

because:

```text
CampusQuest
```

is our overall product/project name, while:

```text
config
```

is the Django configuration package.

This gives us a clean structure:

```text
CampusQuest/
│
├── manage.py
│
└── config/
    ├── settings.py
    ├── urls.py
    ├── wsgi.py
    └── asgi.py
```

Later:

```text
CampusQuest/
│
├── config/
├── accounts/
├── challenges/
├── leaderboard/
└── ...
```

---

# 16. What Does `.` Mean?

The dot:

```text
.
```

means:

> **Current directory**

We are already inside:

```text
C:\Projects\CampusQuest
```

Therefore:

```cmd
django-admin startproject config .
```

means:

> Create the Django project configuration named `config` in the current CampusQuest directory.

---

# 17. Why We Use `.`

Suppose we run:

```cmd
django-admin startproject config
```

Django creates the project inside a new directory named `config`.

For our course, we want:

```text
CampusQuest/
├── manage.py
└── config/
```

Therefore we use:

```cmd
django-admin startproject config .
```

The `.` tells Django to use the current directory.

---

# 18. Check the Directory

Run:

```cmd
dir
```

You should now see something similar to:

```text
CampusQuest
│
├── venv
├── requirements.txt
├── manage.py
└── config
```

---

# 19. Our First Django Project Structure

We now have:

```text
CampusQuest/
│
├── venv/
│
├── requirements.txt
│
├── manage.py
│
└── config/
    ├── __init__.py
    ├── settings.py
    ├── urls.py
    ├── wsgi.py
    └── asgi.py
```

This is the basic Django project structure.

---

# PART C — UNDERSTANDING EVERY FILE

# 20. `manage.py`

The first important file is:

```text
manage.py
```

It is located here:

```text
CampusQuest/
└── manage.py
```

---

# 21. What Is `manage.py`?

`manage.py` is the project's command-line utility.

It allows us to execute Django commands for our project.

For example:

```cmd
python manage.py runserver
```

Other commands we will use later:

```cmd
python manage.py startapp accounts
```

```cmd
python manage.py makemigrations
```

```cmd
python manage.py migrate
```

```cmd
python manage.py createsuperuser
```

---

# 22. Simple Way to Remember `manage.py`

Think:

> **`manage.py` = remote control for your Django project**

It helps us perform project operations.

```text
manage.py
    |
    +── runserver
    +── startapp
    +── migrate
    +── makemigrations
    +── createsuperuser
    +── shell
```

---

# 23. Why Use `python manage.py`?

We normally use:

```cmd
python manage.py runserver
```

because `manage.py` knows which Django project configuration should be used.

---

# 24. `config` Directory

Inside the project:

```text
config/
```

we have:

```text
config/
├── __init__.py
├── settings.py
├── urls.py
├── wsgi.py
└── asgi.py
```

This directory contains the **project configuration**.

It is not the same thing as a Django application.

---

# 25. `__init__.py`

File:

```text
config/__init__.py
```

It tells Python that the directory can be treated as a Python package.

Usually, we don't need to modify this file.

---

# 26. `settings.py`

File:

```text
config/settings.py
```

This is one of the most important Django files.

It contains project configuration.

Examples include:

```text
SECRET_KEY
DEBUG
ALLOWED_HOSTS
INSTALLED_APPS
MIDDLEWARE
ROOT_URLCONF
TEMPLATES
WSGI_APPLICATION
DATABASES
LANGUAGE_CODE
TIME_ZONE
STATIC_URL
```

---

# 27. Think of `settings.py` as the Control Room

Imagine CampusQuest is a large company.

```text
settings.py
      |
      +── Security
      +── Database
      +── Apps
      +── Middleware
      +── Templates
      +── Static files
      +── Language
      +── Time zone
```

It controls how Django behaves.

---

# 28. `SECRET_KEY`

Inside `settings.py` you will see:

```python
SECRET_KEY = '...'
```

This is an important security setting.

Later we will move sensitive configuration into environment variables using:

```text
.env
```

and:

```text
python-dotenv
```

We will handle this properly when we configure the project.

---

# 29. `DEBUG`

You will see:

```python
DEBUG = True
```

During development, Django uses debug mode to provide useful error information.

For production, this must be configured appropriately.

> Never treat a development `DEBUG=True` configuration as a production configuration.

---

# 30. `ALLOWED_HOSTS`

You will see:

```python
ALLOWED_HOSTS = []
```

This controls which host/domain names can serve the Django application.

We will study it later when discussing deployment and security.

---

# 31. `INSTALLED_APPS`

You will see:

```python
INSTALLED_APPS = [
    ...
]
```

Django registers applications here.

Later, when we create:

```text
core
```

we will add it to `INSTALLED_APPS`.

---

# 32. `MIDDLEWARE`

You will see:

```python
MIDDLEWARE = [
    ...
]
```

Middleware processes requests and responses as they travel through Django.

Think:

```text
Browser
   ↓
Middleware
   ↓
View
   ↓
Middleware
   ↓
Browser
```

We will study middleware in detail later.

---

# 33. `ROOT_URLCONF`

You will see something similar to:

```python
ROOT_URLCONF = 'config.urls'
```

This tells Django where the project's main URL configuration is located.

That file is:

```text
config/urls.py
```

---

# 34. `TEMPLATES`

The `TEMPLATES` configuration tells Django how templates are configured.

Templates are used to generate HTML pages.

We will study templates later.

---

# 35. `DATABASES`

You will see a default database configuration.

Django initially creates a configuration suitable for SQLite.

For example:

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}
```

Our CampusQuest project will eventually use:

```text
MySQL
```

We will configure MySQL in a later lesson.

For now:

> **Do not change the database configuration on Day 3 unless instructed.**

---

# 36. `urls.py`

File:

```text
config/urls.py
```

This is the project's main URL configuration.

A URL tells Django:

> **When the user visits this URL, what should Django do?**

For example:

```text
/              → Home
/about/        → About
/contact/      → Contact
/challenges/   → Challenges
```

---

# 37. Basic URL Flow

Think:

```text
Browser
   |
   | GET /about/
   v
config/urls.py
   |
   v
Find matching URL
   |
   v
View
   |
   v
Response
```

We will learn URL routing properly in upcoming days.

---

# 38. `wsgi.py`

File:

```text
config/wsgi.py
```

WSGI means:

> **Web Server Gateway Interface**

It provides a standard interface between Django and WSGI-compatible web servers.

Conceptually:

```text
Web Server
    ↓
WSGI
    ↓
Django
```

For Day 3:

> Understand its purpose; don't worry about memorizing the internal code.

---

# 39. `asgi.py`

File:

```text
config/asgi.py
```

ASGI means:

> **Asynchronous Server Gateway Interface**

It is the modern interface for asynchronous Python web applications.

Conceptually:

```text
ASGI Server
    ↓
ASGI
    ↓
Django
```

It is useful for asynchronous capabilities and protocols supported by ASGI-based systems.

For Day 3:

> Understand the purpose; don't worry about the internal implementation.

---

# 40. WSGI vs ASGI

| WSGI | ASGI |
|---|---|
| Older Python web-server interface | Modern Python web-server interface |
| Primarily synchronous | Supports asynchronous applications |
| Common in traditional Django deployments | Useful for async Django |
| `wsgi.py` | `asgi.py` |

Simple memory trick:

```text
WSGI → Traditional web interface
ASGI → Async-capable web interface
```

---

# PART D — HOW DJANGO STARTS

# 41. What Happens When We Run Django?

When we execute:

```cmd
python manage.py runserver
```

Django starts the development server.

Conceptually:

```text
manage.py
    ↓
Django
    ↓
settings.py
    ↓
urls.py
    ↓
Applications
    ↓
Views
    ↓
Response
```

---

# 42. Start the Development Server

Make sure you are inside:

```text
C:\Projects\CampusQuest
```

and the environment is activated.

Run:

```cmd
python manage.py runserver
```

You should see output similar to:

```text
Watching for file changes with StatReloader
Performing system checks...

System check identified no issues.

Starting development server at http://127.0.0.1:8000/
Quit the server with CTRL-C.
```

The exact output may vary depending on your Django version.

---

# 43. What Is `127.0.0.1`?

You will see:

```text
127.0.0.1
```

This is the loopback address.

It refers to:

> **Your own computer.**

Another common name is:

```text
localhost
```

So:

```text
127.0.0.1
```

and:

```text
localhost
```

commonly refer to your local machine.

---

# 44. What Is Port `8000`?

The complete development URL is:

```text
http://127.0.0.1:8000/
```

Here:

```text
127.0.0.1
```

means your computer.

```text
8000
```

is the port on which Django's development server is listening.

Think of a port as a numbered communication door.

```text
Computer
│
├── Port 80
├── Port 443
├── Port 3306
└── Port 8000 ← Django development server
```

MySQL commonly uses:

```text
3306
```

Django's development server commonly uses:

```text
8000
```

---

# 45. Open the Django Welcome Page

Open your browser and visit:

```text
http://127.0.0.1:8000/
```

You should see Django's default welcome page.

This confirms:

```text
Python
   ↓
Django
   ↓
Project
   ↓
Development server
   ↓
Browser
```

is working.

---

# 46. Alternative URL

You can also try:

```text
http://localhost:8000/
```

Both commonly point to your local machine.

---

# 47. Stop the Development Server

Return to Command Prompt.

Press:

```text
CTRL + C
```

The development server will stop.

---

# 48. Start It Again

Run:

```cmd
python manage.py runserver
```

Again open:

```text
http://127.0.0.1:8000/
```
