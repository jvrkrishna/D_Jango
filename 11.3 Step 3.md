# Django Forms — Form Field Options

## 1. Create the Form

### `students/forms.py`

```python
from django import forms


# Form Field Options
class StudentForm(forms.Form):

    name = forms.CharField(
        required=True,
        label="Student Name",
        initial="John",
        help_text="Enter your full name.",
        error_messages={
            "required": "Please enter your name."
        },
        widget=forms.TextInput()
    )

    dob = forms.DateField(
        required=True,
        label="Date of Birth",
        help_text="Enter your date of birth.",
        widget=forms.DateInput(
            attrs={"type": "date"}
        )
    )

    gender = forms.ChoiceField(
        required=True,
        label="Gender",
        choices=[
            ("Male", "Male"),
            ("Female", "Female"),
            ("Others", "Others"),
        ],
        help_text="Select your gender."
    )
```

---

## 2. `required`

```python
required=True
```

```python
name = forms.CharField(
    required=True
)
```

```python
required=False
```

---

## 3. `label`

```python
label="Student Name"
```

```python
name = forms.CharField(
    label="Student Name"
)
```

---

## 4. `initial`

```python
initial="John"
```

```python
name = forms.CharField(
    initial="John"
)
```

---

## 5. `help_text`

```python
help_text="Enter your full name."
```

```python
name = forms.CharField(
    help_text="Enter your full name."
)
```

---

## 6. `error_messages`

```python
error_messages={
    "required": "Please enter your name."
}
```

```python
name = forms.CharField(
    error_messages={
        "required": "Please enter your name."
    }
)
```

---

## 7. `widget`

```python
widget=forms.TextInput()
```

```python
name = forms.CharField(
    widget=forms.TextInput()
)
```

---

## 8. Date Widget

```python
widget=forms.DateInput(
    attrs={"type": "date"}
)
```

```python
dob = forms.DateField(
    widget=forms.DateInput(
        attrs={"type": "date"}
    )
)
```

---

## 9. Choice Field

```python
gender = forms.ChoiceField(
    required=True,
    label="Gender",
    choices=[
        ("Male", "Male"),
        ("Female", "Female"),
        ("Others", "Others"),
    ],
    help_text="Select your gender."
)
```

---

## 10. Complete Form

```python
from django import forms


class StudentForm(forms.Form):

    name = forms.CharField(
        required=True,
        label="Student Name",
        initial="John",
        help_text="Enter your full name.",
        error_messages={
            "required": "Please enter your name."
        },
        widget=forms.TextInput()
    )

    dob = forms.DateField(
        required=True,
        label="Date of Birth",
        help_text="Enter your date of birth.",
        widget=forms.DateInput(
            attrs={"type": "date"}
        )
    )

    gender = forms.ChoiceField(
        required=True,
        label="Gender",
        choices=[
            ("Male", "Male"),
            ("Female", "Female"),
            ("Others", "Others"),
        ],
        help_text="Select your gender."
    )
```

---

## 11. Template

### `students/templates/student_form.html`

```html
<!DOCTYPE html>
<html>
<head>
    <title>Student Form</title>
</head>
<body>

    <h1>Student Registration</h1>

    <form method="post">

        {% csrf_token %}

        {{ form.as_p }}

        <button type="submit">Submit</button>

    </form>

</body>
</html>
```

---

## 12. Views

### `students/views.py`

```python
from django.shortcuts import render, redirect

from .forms import StudentForm


def student_form(request):

    if request.method == "POST":

        form = StudentForm(request.POST)

        if form.is_valid():

            print(form.cleaned_data)

            return redirect("success")

    else:

        form = StudentForm()

    return render(
        request,
        "student_form.html",
        {"form": form}
    )


def success(request):

    return render(
        request,
        "success.html"
    )
```

---

## 13. URLs

### `students/urls.py`

```python
from django.urls import path

from . import views


urlpatterns = [

    path(
        "",
        views.student_form,
        name="student_form"
    ),

    path(
        "success/",
        views.success,
        name="success"
    ),

]
```

### `django_forms_project/urls.py`

```python
from django.contrib import admin
from django.urls import include, path


urlpatterns = [

    path(
        "admin/",
        admin.site.urls
    ),

    path(
        "student/",
        include("students.urls")
    ),

]
```

---

## 14. Success Template

### `students/templates/success.html`

```html
<!DOCTYPE html>
<html>
<head>
    <title>Success</title>
</head>
<body>

    <h1>Registration Successful!</h1>

    <p>Student form submitted successfully.</p>

    <a href="{% url 'student_form' %}">
        Register Another Student
    </a>

</body>
</html>
```

---

## 15. Run Server

```bash
python manage.py runserver
```

```text
http://127.0.0.1:8000/student/
```

---

## 16. Field Options Quick Reference

```python
forms.CharField(
    required=True,
    label="Student Name",
    initial="John",
    help_text="Enter your full name.",
    error_messages={
        "required": "Please enter your name."
    },
    widget=forms.TextInput()
)
```

```python
forms.DateField(
    required=True,
    label="Date of Birth",
    help_text="Enter your date of birth.",
    widget=forms.DateInput(
        attrs={"type": "date"}
    )
)
```

```python
forms.ChoiceField(
    required=True,
    label="Gender",
    choices=[
        ("Male", "Male"),
        ("Female", "Female"),
        ("Others", "Others"),
    ],
    help_text="Select your gender."
)
```
